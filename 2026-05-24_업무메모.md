# 📋 C-SAFE 내일 업무 메모
> 작성일: 2026-05-23 (오늘) | 실행일: 2026-05-24

---

## ✅ 오늘 완료된 것 (참고용)
- GPS 중복 워처 제거 (`useHazardWarning` → `useGeolocation` 구독)
- `useNearMissEngine` 아차사고 이벤트 수집 훅 생성
- `AISafetyCoach` 주행 후 AI 코칭 컴포넌트 생성
- README 전면 업데이트 → GitHub + Vercel 배포 완료

---

## 🔴 1순위 — Supabase 마이그레이션 실행 (5분)

**아차사고 데이터가 실제로 DB에 저장되려면 반드시 먼저 실행해야 함**

1. Supabase Dashboard 접속 → https://supabase.com
2. 해당 프로젝트 → `SQL Editor` 탭 클릭
3. 아래 파일 내용 복사 후 실행:
   ```
   c-pm-safety/supabase/migrations/supabase_near_miss_events.sql
   ```
4. `near_miss_events` 테이블 생성 확인 (Table Editor에서 확인)

---

## 🟠 2순위 — 온보딩 상태 영속화 (C3 버그 수정, 30분)

**현재 문제**: 앱 새로고침 시마다 언어선택 → 약관 → 퀴즈를 반복해야 함  
**목표**: 한 번 완료하면 다시 안 보이게

- `hasAgreedDisclaimer` → `useLocalStorage` 훅으로 교체
- `hasCompletedOnboarding` → `useLocalStorage` 훅으로 교체
- (`useLocalStorage` 훅은 이미 존재하므로 그대로 사용)
- 수정 파일: `App.jsx` 92~95번 줄

---

## 🟡 3순위 — 초보자 미션 시스템 신규 개발 (2~3시간)

**목표**: 게임화로 장기 재방문 유도

새로 만들 것:
- `useBeginnerMissions.js` 훅 (미션 상태 + 진행도 localStorage 저장)
- `BeginnerMissionCard.jsx` 컴포넌트 (UserProfileSheet 내부 탭에 추가)

미션 예시 (하드코딩 시작, 나중에 DB 연동):
```
미션 1: 첫 안전 주행 완료          → +200P
미션 2: 헬멧 인증 3회 연속         → +300P  ★ 스트릭
미션 3: 급제동 없이 5km 주행       → +500P
미션 4: 7일 연속 주행              → +1000P ★ 스트릭
미션 5: 위험 구역 신고 1회         → +150P
```

---

## 🟢 4순위 — ShadowImpactSheet 실데이터 연동 (1~2시간)

**현재 상태**: `ShadowImpactSheet`가 목업 애니메이션만 표시  
**목표**: 실제 주행 기록 + 아차사고 데이터 시각화

- `useRideSession`의 `rideHistory` 데이터 연결
- `useNearMissEngine`의 `getLocalNearMisses()` 연결
- 미니 맵 시각화: 실제 `ride_paths` 경로 데이터로 SVG 경로 그리기
- 수정 파일: `ShadowImpactSheet.jsx` + `App.jsx` (props 추가)

---

## 🔵 5순위 — AdminDashboard 레이지 로딩 (30분)

**현재 문제**: 39KB AdminDashboard가 앱 시작 시 항상 번들에 포함됨  
**목표**: 관리자가 열 때만 로드

```js
// App.jsx 상단 import 변경
// 기존:
import AdminDashboard from './components/admin/AdminDashboard';

// 변경:
const AdminDashboard = React.lazy(() => import('./components/admin/AdminDashboard'));
// + <Suspense fallback={<div>...</div>}>로 감싸기
```

---

## ⚪ 여유 시 진행 — 개인 위험 프로파일 카드

**목표**: 사용자별 분석 카드 (UserProfileSheet 내)

표시 항목:
- 주간 급제동 추세 (증가/감소%)
- 야간 주행 비율
- 위험 구역 통과 횟수
- 반응 속도 개선 지수

수집 데이터 소스: `rideHistory` + `near_miss_events` localStorage

---

## 📌 참고 — 절대 건드리지 말 것

```
useRideSession.js      ← 주행 상태 머신
useUserStore.js        ← 인증/프로필
useGeolocation.js      ← GPS 단일 인스턴스
lib/supabaseClient.js  ← DB 싱글턴
MapContainer.jsx       ← 카카오맵 52KB
AdminDashboard.jsx     ← 5순위 레이지 로딩 전까지 건드리지 말 것
```

---

## 📊 내일 예상 결과

| 완료 시 | 효과 |
|---------|------|
| 1순위 완료 | 아차사고 데이터 실제 수집 시작 |
| 2순위 완료 | 기존 사용자 온보딩 반복 문제 해소 |
| 3순위 완료 | 재방문율 향상 + 데이터 수집량 증가 |
| 4순위 완료 | Shadow Impact가 실제 데이터 기반 뷰로 승격 |
| 5순위 완료 | 초기 번들 약 39KB 감소 |

---

*메모 생성: Antigravity AI | C-SAFE v3.0*
