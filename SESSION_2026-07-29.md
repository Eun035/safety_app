# C-Safe 세션 작업 정리 — 2026-07-29

> 라이드 서머리 **버그 수정 + 개인정보·데이터 시각화 개선**, 그리고 **Supabase CLI 도입(로컬↔운영 마이그레이션 연동)** 및 로컬 개발 환경 구축을 한 파일로 정리.

---

## TL;DR

- **라이드 서머리 미표시 버그 수정** — 결제 영수증에서 '나중에'/닫기로 빠져나오면 뒤 체인(스테이션 보상→서머리)이 끊겨 서머리가 안 뜨던 문제
- **공유 카드 개인정보 보호** — SNS 공유 이미지에서 **도착 장소(목적지) 제외**
- **스피드 히트 루트** — 공유 카드 경로를 **속도별 색 그라디언트**(느림→빠름) + ⚡최고속도 마커 + 범례로 업그레이드
- **부드러운 베지어(S자) 곡선** — 직선 꺾은선 → Catmull-Rom 스무딩으로 chill한 라이드보고서
- **경로 선 스타일 선택** — 굵기 + 선 종류(실선/점선/도트/이중선) 칩 선택
- **Supabase CLI 도입** — 로컬 개발 DB ↔ 운영 마이그레이션 연동 + 로컬 스키마 baseline 복원(실동작 확인)
- **CLI 실행 이슈 해결** — npx의 supabase.exe가 백신에 차단 → **Scoop 네이티브 설치**로 전환
- **`npm run dev` 시 로컬 DB 자동 기동** — `predev` 훅(supabase start)
- 커밋 10개 push (`8b96451` → `3dc8d31`), 모두 `main` 반영

---

## 1. 라이드 서머리 미표시 버그 (`8b96451`)

- 증상: 주행 종료 후 결제·보상 모달은 뜨는데 **라이드 서머리가 안 뜸**.
- 원인: 체인이 **헬멧반납 → 결제 → 스테이션보상 → 서머리** 순인데, `PaymentReceiptModal`의 **"나중에 결제"/닫기(onClose)** 가 결제창만 닫고 다음 단계로 넘기지 않았음. 오직 "결제하기" 버튼을 눌러야만 진행됐음.
- 해결: `App.jsx`의 `PaymentReceiptModal onClose`를 결제 완료(`handlePaymentComplete`)와 동일 처리 → 어떤 방식으로 결제 단계를 벗어나도 서머리까지 도달.

---

## 2. 공유 카드 — 개인정보 & 데이터 시각화

### 2.1 도착 장소 제외 (`0a4bbdf`)
- SNS로 나가는 공유 카드 제목에서 **실제 목적지 이름 제거** → 일반 라벨(`sc_route_label`)로 대체.
- `renderShareCard` 호출 시 `routeLabel: null` 전달. 내 화면 미리보기는 그대로.

### 2.2 스피드 히트 루트 (`89fb7d8`)
- 경로 라인을 **속도에 따라 색이 변하는 그라디언트**로 렌더 — 느림 = 테마 보조색(accent2) → 빠름 = 강조색(accent). (레인보우 금지, **순차 인코딩**)
- **글로우 언더레이** + **⚡ 최고속도 지점 마커/라벨** + **우상단 SLOW→FAST 범례**.
- 데이터 흐름: `useRideSession`이 경로에 **ts(시간) 유지** → `routeSketch`가 구간 속도(km/h, 3점 이동평균)·min/max/maxIndex 산출 → `renderShareCard`가 세그먼트별 색 렌더.
- 속도 정보 없는 짧은 주행/구 기록은 **단색 폴백**. (속도 색은 이 변경 이후 새 주행부터 적용)

### 2.3 부드러운 베지어(S자) 곡선 (`67ce1f4`)
- GPS 점을 **직선으로 잇던 꺾은선 → Catmull-Rom → 3차 베지어 스무딩**으로 흐르는 곡선화(chill한 감성).
- GPS 노이즈 완화를 위해 점을 **균일 다운샘플(최대 64점)**.
- `routeSketch`가 부드러운 `d`(미리보기 SVG)와 `curve`(캔버스 세그먼트)를 반환 → 공유 카드·미리보기 모두 곡선.

### 2.4 경로 선 스타일 선택 (`de4490d`)
- **굵기 + 선 종류** 선택 칩: `얇게/기본/굵게/점선/도트/이중선`(`SHARE_LINE_STYLES`).
- 이중선은 배경톤 중심선 오버레이로 표현, 칩은 CSS `border-top-style`로 실물 미리보기.
- 미리보기 곡선에도 굵기/대시 즉시 반영. i18n: `rsm_line_style`, `rsm_ls_*`.
- (버그) 즉석 렌더 폴백 경로에 남아있던 **목적지 누출** 함께 수정(`routeLabel: null`).

---

## 3. Supabase CLI 도입 — 로컬↔운영 마이그레이션 연동

### 3.1 스캐폴딩 (`390c645`)
- `supabase init` → `supabase/config.toml`(익명 로그인 활성화, 시드 경로, major_version).
- 수작업 SQL 15개 + `supabase_referrals.sql` → `supabase/migrations_legacy/` 로 아카이브.
- `package.json`에 `db:start/stop/status/reset/new/diff/pull/push/link` 스크립트.
- `supabase/README.CLI.md`(link→pull→start→push 워크플로), 백업용 셀프호스팅 스택은 `infra/supabase/`에 보존(`DEPRECATED.md`).

### 3.2 로컬 스키마 baseline 복원 (`dba1cbc`)
- **문제**: 링크한 운영 프로젝트가 **정지(paused) 상태라 앱 테이블이 없어**, `db pull` baseline이 거의 비어 있었음.
- **해결**: `migrations_legacy/`의 실제 스키마 15개 + 확장(uuid-ossp/postgis)을 **의존성 순서 timestamp 마이그레이션**(`20260728120000~120015`)으로 복원 → `npm run db:reset`로 로컬에 전체 테이블 생성 확인(rides/profiles/hazards/near_miss_events/zone_events/ride_paths/safety_grid_scores/feedbacks/referrals).
- 시드 비활성(옛 목업이 실제 스키마와 컬럼 불일치).
- 실제 셋업에서 겪은 이슈(프로젝트 정지·잘못된 ref·DB 비밀번호 특수문자·빈 baseline·로컬 카카오맵 키)를 `supabase/README.CLI.md` 6절에 문서화.

### 3.3 CLI 실행 이슈 → Scoop 네이티브 설치 (`e63e6d8`)
- **문제**: `npm run db:start`가 `spawnSync ... supabase.exe UNKNOWN`(errno -4094) / 직접 실행 시 `Permission denied`.
  npx가 받아둔 `supabase.exe`를 **Windows 백신이 실행 차단**(Defender 비활성화해도 지속).
- **해결**: **Scoop으로 supabase CLI 네이티브 설치**(`scoop bucket add supabase … && scoop install supabase`) → `supabase --version` 정상.
  `package.json`의 `db:*`를 `npx --yes supabase` → **`supabase`(PATH)** 로 전환. README에 Scoop 설치 절차(0절) 추가.
- ⚠️ 설치 후 **새 터미널**이어야 PATH(scoop shims) 반영됨.

### 3.4 `npm run dev` 시 로컬 DB 자동 기동 (`3dc8d31`)
- **`predev` 훅**: `supabase start` → `npm run dev` 하나로 **로컬 DB + 프론트** 함께 기동(`supabase start`는 멱등).
- `dev:web` = vite 단독(프론트만) 이스케이프 해치.

### 3.5 로컬 개발 흐름 (요약)
```bash
npm run dev        # predev(supabase start) → vite. 앱 http://localhost:8888
npm run dev:web    # 프론트만(DB 없이)
# 관리화면: http://127.0.0.1:54323 / DB만: db:start·db:stop
# 운영 반영: db:new → db:reset(로컬검증) → db:push
```
> 전제: **Docker Desktop 실행 + supabase CLI(Scoop) 설치 + 새 터미널**.

---

## 4. 주요 파일

| 파일 | 변경 |
|------|------|
| `src/App.jsx` | 결제 onClose → 서머리 체인 연결 |
| `src/components/common/RideSummaryModal.jsx` | 공유 시 목적지 제외(routeLabel null) |
| `src/utils/routeSketch.js` | ts 기반 구간 속도 산출 + 다운샘플·베지어 스무딩·curve 반환 |
| `src/utils/renderShareCard.js` | 스피드 히트 루트·베지어 곡선 렌더 + 선 스타일(굵기/실선·점선·도트·이중선) |
| `src/components/common/RideSummaryModal.jsx` | 공유 시 목적지 제외, 선 스타일 선택 칩·미리보기 반영 |
| `src/hooks/useRideSession.js` | 경로에 ts 유지 |
| `src/locales/i18n.js` | `rsm_line_style`·`rsm_ls_*` (4개 언어) |
| `package.json` | `db:*` → `supabase`(PATH), `predev`(자동 기동), `dev:web` |
| `supabase/config.toml`, `supabase/migrations/*`, `supabase/README.CLI.md` | CLI 도입·baseline·Scoop·문서 |
| `infra/supabase/` | 백업용 셀프호스팅 스택(보존) |

---

## 5. 커밋 (`8b96451` → `3dc8d31`, 10개)

- `8b96451` 결제 '나중에'/닫기 시 라이드 서머리 미표시 수정
- `390c645` Supabase CLI 도입 스캐폴딩
- `dba1cbc` CLI 로컬 스키마 baseline 복원 + 셋업 문서화
- `0a4bbdf` 공유 카드 도착 장소 제외(개인정보)
- `89fb7d8` 스피드 히트 루트(속도별 색 경로 + ⚡ 마커)
- `6f43496` SESSION_2026-07-29 + README 갱신(초안)
- `67ce1f4` 라이드 경로 부드러운 베지어(S자) 곡선
- `de4490d` 경로 선 스타일 선택(굵기 + 선 종류) + 폴백 목적지 누출 수정
- `e63e6d8` supabase CLI를 Scoop 네이티브 설치 기반으로 전환
- `3dc8d31` `npm run dev` 시 로컬 DB 자동 기동(predev)

---

## 6. 참고 / 남은 것

- **속도 색·곡선은 새 주행부터** 예쁘게 적용(경로 ts 저장 시작). 기존 기록은 단색·직선.
- **로컬 지도(카카오맵)**: `.env.local`에 카카오 키 추가 + developers.kakao.com에 `http://localhost:8888` 도메인 등록 필요.
- **로컬 DB 실행 전제**: Docker Desktop 실행 + supabase CLI(Scoop) 설치 + **새 터미널**(PATH 반영).
- **운영 DB 비어 있음** → 필요 시 `npm run db:push`로 스키마 반영.
- **[알려진 관찰] 이동거리/평균속도 과대** — `calculateDistance`가 미터를 반환하는데 앱이 km로 취급 → 거리 1000배, 그로 인해 평균속도도 폭증("광속 라이딩" 밈). **의도적으로 그대로 둠**(재미 요소). 정식화 시 거리 누적에 `/1000`.
- 후속 옵션: 미리보기 경로 속도 색 통일, 안전점수 원형 게이지, 속도별 굵기(테이퍼) 라인.
