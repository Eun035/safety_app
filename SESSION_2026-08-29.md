# C-Safe 세션 작업 정리 — 2026-08-29

> lint 정리로 시작했으나, 진행 과정에서 **운영에 실제 영향을 주던 결함 7건**이 드러나 함께 수정.
> 성능 개선과 운영 DB 정합화까지 한 파일로 정리.

---

## TL;DR

- **lint 에러 58 → 0, 경고 14 → 0** (일괄 억제가 아니라 건별 판단)
- **초기 임계 경로 gzip 479KB → 314KB (−34%)**
- **운영 DB에 `feedbacks` 테이블이 없어** 탈퇴 사유 수집이 계속 실패하던 것을 복구
- **Supabase CLI가 잘못된 프로젝트에 링크**돼 있던 것을 바로잡고 마이그레이션 baseline 정리
- 기상 감지를 **위치 기반**으로 전환 + 근거 없는 가짜 경고 제거
- i18n 언어별 분리 후 생긴 **백지 화면 위험**을 스스로 잡아냄
- 커밋 9개, 프로덕션 배포 2회

---

## 1. 운영 DB — 링크 오류와 누락 테이블 (`865ddf7`)

### 1.1 CLI가 다른 프로젝트를 보고 있었다
Supabase 프로젝트가 **두 개**였다.

| 프로젝트 | ref | 상태 | 비고 |
|---|---|---|---|
| **c-safe-backend** | `zivkvzqulgzkqdgpboxg` | ACTIVE | **실제 운영.** Vercel의 `VITE_SUPABASE_URL`이 가리킴 |
| Eun035's Project | `glzeykoghbljvxmxrcqu` | INACTIVE | 미사용. **CLI가 여기 링크돼 있었음** |

- SESSION_2026-07-29의 "운영 DB 비어 있음 / 프로젝트 정지" 관찰은 이 오링크 때문이었다.
- 운영은 멀쩡히 가동 중이었고 데이터도 쌓이고 있었다 — profiles 70행, referrals 3행, near_miss_events 10행, safety_grid_scores 54행.

### 1.2 `feedbacks` 테이블이 운영에 없었다 (실제 결함)
- Phase 19에서 정의됐으나(`20260728120013`) 운영에 **생성된 적이 없음**.
- `AccountDeletionModal`이 탈퇴 사유를 이 테이블에 INSERT → **계속 실패**. 지금까지의 탈퇴 사유는 유실.
- 신규 마이그레이션 `20260829123930_create_feedbacks_table.sql` 작성 → 로컬 `db reset` 검증 → 운영 push.
- ⚠️ `uuid_generate_v4()`는 운영에서 uuid-ossp가 `extensions` 스키마에 있어 search_path에 안 잡힘 → **`gen_random_uuid()`**(PG13+ 내장) 사용.

### 1.3 마이그레이션 이력 baseline
- 운영엔 테이블만 손으로 만들어져 있고 이력이 전혀 없었다.
- 기존 17개를 `migration repair --status applied`로 baseline 처리(메타데이터만 변경) → `db pull`로 실제 스키마 캡처(`20260829123826_remote_schema.sql`) → 로컬↔운영 일치.

> **교훈**: DB 작업 전 `supabase projects list`로 `linked:true` 대상을 확인하고,
> `supabase inspect db table-stats`로 실제 테이블·행 수를 먼저 볼 것.

---

## 2. 고친 실제 결함

| # | 결함 | 커밋 |
|---|---|---|
| 1 | 운영 `feedbacks` 테이블 누락 → 탈퇴 사유 수집 실패 | `865ddf7` |
| 2 | **지역 전환 버그** — 천안↔아산 전환해도 PM 스테이션·장소 검색이 이전 지역 좌표 기준 | `1dbf88c` |
| 3 | **기상 감지가 천안 고정 좌표** + 초기 1회만 조회 | `d32f690` |
| 4 | **API 실패 시 `Math.random() < 0.3`으로 가짜 기상 경고** 생성 | `d32f690` |
| 5 | **카카오 검색 레이스** — 응답 역전 시 이전 검색어 결과가 화면에 남음 | `2880f81` |
| 6 | **번역 사전 로딩 hang 시 백지 화면** (이번에 도입했다가 발견) | `9568b3b` |
| 7 | **추천 가입 연결이 조용히 실패** | `5c4fb0e` |

### 2.1 지역 전환 버그
`MapContainer`의 PM 스테이션 조회·장소 검색이 `regionMeta.center` 반경 기준인데
effect 의존성에 빠져 있어 지역을 바꿔도 재조회되지 않았다.

### 2.2 기상 감지 (`d32f690`)
- `useSafeData`가 **천안 단대호수 고정 좌표(36.833, 127.179)** 로만 조회 → 아산에서 주행해도 천안 날씨로 판단.
- 초기 1회만 조회 → 주행 중 비가 와도 감지 안 됨.
- 실패 시 **30% 확률로 위험 경고를 만들어내던 테스트 기믹** 존재. 근거 없는 안전 경고는 경고 자체의 신뢰를 무너뜨리므로 제거(실패 시 경고 안 함 + 다음 주기 재시도).
- 수정: `useGeolocation` 현재 위치 기준(없으면 `useRegion` 중심 폴백), **10분 주기 갱신**,
  좌표를 **약 5km 격자로 스냅**해 초 단위 GPS마다 API를 두드리지 않게 함.

### 2.3 백지 화면 (`9568b3b`) — 이번에 만들었다가 잡은 것
- i18n 동적 로딩(`e5e27bf`) 도입 시 첫 렌더가 사전 로딩을 기다리게 했는데, **`import()`에는 타임아웃이 없다.**
- 통신이 멈추면 Promise가 resolve도 reject도 되지 않아 `catch`로도 못 잡고 → **화면이 영영 백지**.
- 수정: `i18nReady`를 2.5초 타임아웃과 race + `main.jsx`에 5초 최후 안전장치·이중 렌더 방어.
  늦게 온 사전은 도착 즉시 `changeLanguage`가 적용됨.

### 2.4 추천 가입 연결 (`5c4fb0e`)
- **PostgREST의 UPDATE에는 order/limit이 먹지 않는다.** `.order('landed_at').limit(1)`은 무시되고
  조건에 맞는 **모든 행**을 고치려 들어, 미청구 landing 행이 2개 이상이면
  `invitee_user_id` 유니크 제약 위반으로 **409 실패**.
- 게다가 supabase-js는 예외를 던지지 않아 `try/catch`가 무의미 → 실패해도 로그조차 없음.
- 수정: select로 1건을 고른 뒤 **id로 지정 update**, 각 단계 `error`를 실제로 확인.
- 보상 자체는 `profiles.referred_by_code` 기준 RPC가 처리하므로 지급에는 영향 없었음.

---

## 3. 성능 (`7fabc5d`, `9b20110`, `e5e27bf`)

| | 이전 | 이후 |
|---|---|---|
| 초기 임계 경로 (gzip) | 479 KB | **314 KB** |
| `index.js` (gzip) | 479 KB | 121 KB |
| 청크 | 1개 | 8개 (벤더 분리) |

- **QR 417KB(gzip 113KB)를 초기 로드에서 제거** — `qrcode`는 동적 import,
  `QRScanner`는 `React.lazy` + **열릴 때만 마운트**(lazy만 걸고 항상 렌더하면 첫 렌더에 받아버려 의미 없음).
- **i18n 언어별 분리** — `ko.js/ja.js/en.js/zh-CN.js`. 기본어 ko만 정적, 나머지는 동적 로딩.
  원본과 **4개 언어 2,785개 키 전수 대조**로 완전 일치 확인.
- `manualChunks`로 벤더 분리(react/motion/icons/i18n/supabase/qr) → 앱 코드만 바뀌는 배포에서 캐시 유지.
- 미사용 의존성 제거: `axios`, `html-to-image`, `onnxruntime-web` (번들엔 원래 없었고, 설치 용량 약 8MB 감소).

> **서비스 워커 precache는 유지하기로 결정.** 첫 방문 시 SW가 전 청크를 배경에서 받아
> 오프라인에서도 QR·다국어가 동작한다. 주행 중 통신이 끊길 수 있는 앱 성격상 이쪽이 맞다.
> 즉 **임계 경로는 −34%, 총 다운로드 바이트는 유지**가 정확한 표현.

### i18next 설정에서 주의할 두 가지 (실측으로 확인)
- **`nonExplicitSupportedLngs`** — 지역 코드를 떼어내 비교하는 탓에 `zh-CN`이 영영 매칭되지 않고 ko로 폴백된다. 쓰지 않는다.
- **`supportedLngs`** — 지정하면 init 단계에서 `en-US`가 곧바로 fallback(ko)으로 치환돼 정규화할 원본을 잃는다. 지정하지 않고 `normalizeLanguage`로 직접 처리한다.
- 부수 효과: `zh-TW`가 기존엔 한국어로 떨어지던 것이 이제 중국어에 매칭된다.

---

## 4. 코드 품질 (`2880f81`, `1dbf88c`)

### 4.1 ESLint 설정 자체가 틀려 있었다
`eslint-plugin-react`의 **`jsx-uses-vars`가 없어** `<motion.div>`처럼 JSX에서만 쓰이는 식별자
**16개를 전부 '미사용'으로 오탐**하고 있었다. 이 오탐을 믿고 import를 지우면 앱이 깨진다.

### 4.2 React 19 규칙 위반
- effect 내 동기 setState → **렌더 중 조정** 패턴으로 전환.
  이때 `prev` 상태 초기값을 마운트 시 첫 실행이 유지되도록 잡아야 한다.
- `useSpeechRecognition` 렌더 중 ref 갱신 제거, 지원 여부는 lazy 초기화.
- `HelmetDetectionCamera` cleanup이 참조하는 `stopStream`의 TDZ 위험 제거.
- 죽은 상태 제거(`SafetyQuiz.hasSpokenInit`), 파생 가능한 상태 제거(`HardwareStatusOverlay.stepText`).

### 4.3 exhaustive-deps는 건별 판단
- **채운 곳**: `vibrate`(useCallback으로 안정), `t`, MapContainer useMemo(라벨이 t를 씀).
- **억제한 곳**(이유를 주석으로 명시): `mapBounds`/`regionMeta.center` 객체 동일성 오탐,
  `initAuth` 마운트 1회 부팅 시퀀스, QRScanner(넣으면 스캐너 재시작),
  MapContainer:250(넣으면 언어 변경 시 경로 재계산).

---

## 5. 주요 파일

| 파일 | 변경 |
|------|------|
| `eslint.config.js` | `eslint-plugin-react` + `jsx-uses-vars` 추가 (오탐 제거) |
| `vite.config.js` | `manualChunks` 벤더 분리 |
| `src/main.jsx` | `i18nReady` 대기 후 렌더 + 최후 안전장치·이중 렌더 방어 |
| `src/locales/i18n.js` | 언어별 동적 로딩, `ensureLanguage`/`changeLanguage`/`normalizeLanguage`, 타임아웃 race |
| `src/locales/{ko,ja,en,zh-CN}.js` | 언어별 사전 분리 (신규) |
| `src/hooks/useSafeData.js` | 기상 조회 위치 기반 + 10분 갱신 + 가짜 경고 제거 |
| `src/App.jsx` | QRScanner lazy, 추천 가입 연결 수정, deps 정리 |
| `src/components/map/MapContainer.jsx` | 지역 전환 시 스테이션·검색 재조회 |
| `src/components/map/MapSearchBar.jsx` | 검색 결과 레이스 방지(검색조건 키) |
| `src/components/common/RideStartScreen.jsx` | 동일 레이스 방지 + 렌더 중 조정 전환 |
| `src/utils/referral.js` | `qrcode` 동적 import |
| `supabase/migrations/2026082912*.sql` | 운영 스키마 캡처 + `feedbacks` 복구 |

---

## 6. 커밋 (9개)

- `2880f81` lint 에러 58건 제거 + React 19 규칙 위반 정리
- `865ddf7` 운영 DB `feedbacks` 복구 + 마이그레이션 baseline
- `7fabc5d` 초기 번들 gzip 479KB → 357KB
- `9b20110` 미사용 의존성 3개 제거
- `e5e27bf` i18n 언어별 분리 + 동적 로딩
- `1dbf88c` exhaustive-deps 정리 — 지역 전환 버그 2건 포함
- `d32f690` 기상 위험 감지 위치 기반 + 근거 없는 경고 제거
- `9568b3b` 번역 사전 로딩 hang 시 백지 화면 방지
- `5c4fb0e` 추천 가입 연결이 조용히 실패하던 문제

---

## 7. 검증 방법 (재현용)

- **다국어**: 헤드리스 Chrome + `?lng=` 로 ko/ja/en/zh-CN/en-US/ja-JP/zh-TW/ko-KR/fr 9종 확인.
- **기상 위치 반영**: CDP `Emulation.setGeolocationOverride`로 위치를 바꾼 뒤 요청 URL 캡처.
  (제주 → `33.5/126.55`, 아산 배방 → `36.8/127`)
- **백지 화면**: `dist`를 서빙하되 **언어 청크 응답만 보류**하는 서버를 띄우고,
  `--dump-dom`은 load를 기다려 못 쓰므로 **CDP `Runtime.evaluate`로 벽시계 10초 뒤 DOM**을 읽음.
  → 수정 전 `rendered:false`(본문 0자), 수정 후 `rendered:true`(한국어 표시).
- **추천 연결**: 로컬 Supabase REST에 미청구 행 2건을 만들어 PATCH 비교.
  → order+limit 방식 `409`, id 지정 방식 `204`.

---

## 7-1. 보안 점검 결과

이번 세션 커밋 전체를 대상으로 점검하고, **로컬 DB(운영과 동일 스키마)에서 실제 악용을 재현**해 확인했다.

| # | 문제 | 심각도 | 성격 | 상태 |
|---|---|---|---|---|
| 1 | `profiles` RLS `"Enable update for all" USING (true)` → **누구나 임의 프로필 변조** | HIGH | 기존 결함 | ✅ 수정·배포 |
| 2 | `feedbacks` 공개 조회 → **탈퇴 사유 전체 노출** | MEDIUM | 이번 세션에서 유입 | ✅ 수정·배포 |
| 3 | 클라이언트 하드코딩 관리자 비밀번호 `"admin1234"` | MEDIUM | 기존 결함 | ⏸ **미조치** |

### 1. profiles 임의 변조 (`a9ab076`)
- 재현: 사용자 A의 익명 토큰으로 B의 행을 PATCH → **HTTP 204**, `points` 999999 / `nickname` `PWNED` 변경 성공.
- `points`는 추천(+500P)·주차 보상의 재화 → 보상 체계가 무의미했다. anon 키는 번들에 공개되므로 진입 장벽도 없다.
- 수정: `auth.uid() = id` (USING + WITH CHECK), 무제한 INSERT 정책 제거.
- 호환성: 앱의 profiles 쓰기는 전부 본인 id 기준, 보상 RPC는 `SECURITY DEFINER`.
  적용 후 프로필 70 → 82행으로 생성이 계속 동작함을 확인.

### 2. feedbacks 노출 (`33a1893`)
- Phase 19 원본 SQL의 정책을 그대로 가져왔는데, 주석("관리자/조회용으로만 SELECT 허용")과 달리
  `USING (true)` + anon GRANT였다. 앱은 쓰기만 하므로 클라이언트 조회 경로를 닫았다.

### 3. ⏸ 미조치 — 관리자 역할 부재 (**나중에 처리하기로 함**)
- [`UserProfileSheet.jsx`](src/components/common/UserProfileSheet.jsx)의 관리자 진입이
  **클라이언트 문자열 비교 `pwd === "admin1234"`** 하나로만 막혀 있다. 번들에서 그대로 읽힌다.
- DB에 관리자 역할이 없어 `AdminDashboard`가 동작하는 이유가 곧 **`profiles` SELECT가 열려 있기 때문**이다.
- 그래서 `profiles`의 SELECT는 이번에 조이지 않았다 — 조이면 대시보드가 멈춘다.
- **다음 작업 시 함께 처리할 것**: DB에 관리자 역할 도입 → `profiles` SELECT를 `본인 OR 관리자`로 제한 → 클라이언트 비밀번호 제거.

> 정상 확인: `hazards`·`rides`·`ride_paths`·`near_miss_events`는 RLS가 제대로 걸려 익명 조회가 필터링된다.

---

## 8. 남은 것 / 알려진 사항

- **온디바이스 AI 헬멧 판정은 미동작.** `HelmetDetectionCamera`는 주석 그대로 시뮬레이션이며
  `onnxruntime-web`은 어디서도 import되지 않는다(그래서 이번에 의존성에서 제거).
  `PROPOSAL.md`의 관련 서술을 실제 상태에 맞게 수정해 두었으나 **커밋하지 않고 로컬 보관 중**.
- ~~**[의도적 유지] 이동거리/평균속도 과대**~~ → **수정 완료**. 아래 9절 참조.
- `index.js`가 아직 gzip 121KB — 추가 분할 여지 있음.
- PWA의 ONNX 캐싱 규칙과 `public/models`의 더미 모델(4KB)은 향후 실제 연동 대비로 남겨둠.

---

## 9. 거리 단위 정합화 (2026-08-30)

`calculateDistance`는 **미터**를 반환하는데, 호출부마다 해석이 갈려 있었다.
호출부를 전수 확인해 **잘못된 3곳만** 고쳤다(나머지는 이미 미터로 올바르게 사용 중).

| 위치 | 증상 |
|---|---|
| `App.jsx` 주행 거리 누적 | 미터로 누적한 값을 km로 소비(`mileage` 표시, CO₂ = 거리 × 0.2kg/km) → **거리·평균속도 1000배** |
| `HelmetReturnSheet` ×2 | `calculateDistance(...) * 1000` → 100m 지점이 **100,075m**로 표시 |
| `HelmetStationSelector` | 위와 동일. `distanceM <= maxRadiusMeters` **반경 필터가 항상 실패**해 "반경 내 없음" 폴백이 늘 떴다 |

**그대로 둔 곳**(이미 미터로 올바름): 스트레스존 반경 판정(`radius` 150·200m),
합법 주차 판정(`<= 10`), `useAutoCheckout`(`< 10`), `FavoriteStations`,
`SafeCorridorSheet`, `RideStartScreen.estimateEtaMin`(m/min 기준).

검증:
- 단국대 정문 → 천안터미널 = 3,127m (3.13km, 직선거리로 타당)
- 위도 0.0009° 차이 = 100m 정확
- 스테이션 표시: 수정 후 100m / 수정 전이면 100,075m
- `generateMockRide`는 원래부터 km로 생성 → 실제 주행과 테스트 주행의 단위가 이제 일치

데이터 영향: 운영 `rides`·`ride_paths`는 **0행**이라 마이그레이션 불필요.
단말 `localStorage['csafe_ride_history']`에 남은 기존 기록은 옛 단위(1000배)로 남는다.

## 10. CO₂ 계수 일원화 (2026-08-30)

같은 지표를 화면마다 다른 계수로 계산하고 있었다 — `useRideSession`·`App`·
`ShadowImpactSheet`는 **0.2**, `generateMockRide`는 **0.13**. 둘 다 근거가
적혀 있지 않았고, 그래서 주행 요약과 테스트 주행의 숫자가 어긋날 수 있었다.

`src/config/emissions.js` 하나로 모으고 **근거를 명시**했다. 호출부 7곳 전부 교체.

**채택 값: 0.100 kg/km (순 절감량)**
- 승용차 배출 − PM 자체 배출 = **125.2 g/km − 약 25 g/km ≈ 100 g/km**
- 승용차 125.2 g/km: 2020년 국내 판매 10인승 이하 승용·승합차 **실적 평균**
  (환경부 자동차 온실가스 관리제도). 같은 해 기준치 97 g/km를 29% 초과한
  실측 평균이라 제도 기준치보다 실제 주행에 가깝다.
  [정책브리핑](https://www.korea.kr/news/policyNewsView.do?newsId=148908381)

> ⚠️ **차감값 25 g/km는 유리한 쪽 가정이다.** PM 생애주기 배출은 편차가 크다 —
> 개인 소유·장수명 기기 20~25 g/km, **공유형 30~124 g/km**(제조·재배치가 지배적).
> 재배치가 잦거나 기기 수명이 짧은 공유 운영에서는 순 절감이 훨씬 작아지고
> 최악의 경우 0에 가까워질 수 있다.
> 대외 문서에 인용할 때는 **'승용차 대체 기준 순 절감량, PM 배출 25 g/km 가정'**
> 이라는 전제를 함께 밝혀야 정확하다. 실증에서 실제 기기 수명·재배치 데이터가
> 확보되면 갱신할 것. 계수는 `emissions.js` 한 줄만 고치면 전 화면에 반영된다.
