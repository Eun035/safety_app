# C-Safe 세션 작업 정리 — 2026-08-29 ~ 08-30

> lint 정리로 시작했으나, 진행 과정에서 **운영에 실제 영향을 주던 결함 10건**이 드러나 함께 수정.
> 보안 취약점 2건, 성능 개선, 운영 DB 정합화까지 한 파일로 정리.
>
> 커밋 17개(머지 포함 25) · 프로덕션 배포 6회 · 운영 DB 마이그레이션 4건

---

## TL;DR

| 항목 | 결과 |
|---|---|
| lint | 에러 **58 → 0**, 경고 **14 → 0** |
| 초기 임계 경로 JS | gzip **479 KB → 272 KB (−43%)** |
| 보안 | 임의 프로필 변조·탈퇴 사유 노출 **차단** |
| 운영 DB | 누락 테이블 복구 + 마이그레이션 이력 baseline |

**가장 중요한 발견 3가지**
1. **운영 Supabase 프로젝트를 잘못 보고 있었다** — CLI가 미사용 프로젝트에 링크돼, 이전 세션의 "운영 DB 비어 있음" 기록이 사실이 아니었다.
2. **누구나 남의 프로필을 수정할 수 있었다** — 포인트가 보상 재화인데 RLS가 열려 있었다. 실제로 재현해 확인.
3. **거리·CO₂ 지표가 틀려 있었다** — 주행거리 1000배, CO₂ 계수는 화면마다 제각각.

---

## 1. 운영 DB — 잘못된 링크와 누락 테이블

### 1.1 CLI가 다른 프로젝트를 보고 있었다 (`865ddf7`)

Supabase 프로젝트가 **두 개**였다.

| 프로젝트 | ref | 상태 | 비고 |
|---|---|---|---|
| **c-safe-backend** | `zivkvzqulgzkqdgpboxg` | ACTIVE | **실제 운영.** Vercel의 `VITE_SUPABASE_URL`이 가리킴 |
| Eun035's Project | `glzeykoghbljvxmxrcqu` | INACTIVE | 미사용. **CLI가 여기 링크돼 있었음** |

- SESSION_2026-07-29의 "운영 DB 비어 있음 / 프로젝트 정지" 관찰은 이 오링크 때문이었다.
- 운영은 멀쩡히 가동 중이었고 데이터도 쌓이고 있었다 — profiles 70행, referrals 3행, near_miss_events 10행, safety_grid_scores 54행.
- "supabase_referrals.sql 실행 대기"라는 기록도 같은 이유로 틀렸다. `referrals`는 이미 있었다.

> **교훈**: DB 작업 전 `supabase projects list`로 `linked:true` 대상을 확인하고,
> `supabase inspect db table-stats`로 실제 테이블·행 수를 먼저 볼 것.

### 1.2 `feedbacks` 테이블이 운영에 없었다

- Phase 19에서 정의됐으나(`20260728120013`) 운영에 **생성된 적이 없음**.
- `AccountDeletionModal`이 탈퇴 사유를 여기 INSERT → **계속 실패**. 그동안의 탈퇴 사유는 유실.
- ⚠️ `uuid_generate_v4()`는 운영에서 uuid-ossp가 `extensions` 스키마에 있어 search_path에 안 잡힘
  → **`gen_random_uuid()`**(PG13+ 내장) 사용. 첫 push가 이것 때문에 실패했다.

### 1.3 마이그레이션 이력 baseline

운영엔 테이블만 손으로 만들어져 있고 이력이 전혀 없었다.
기존 17개를 `migration repair --status applied`로 baseline 처리(메타데이터만 변경)
→ `db pull`로 실제 스키마 캡처 → 로컬↔운영 일치.

---

## 2. 보안 — 실제 악용을 재현해 확인

주장에 그치지 않고 **로컬 DB(운영과 동일 스키마)에서 직접 재현**했다. 운영 데이터는 건드리지 않았다.

| # | 문제 | 심각도 | 성격 | 상태 |
|---|---|---|---|---|
| 1 | `profiles` RLS `"Enable update for all" USING (true)` → **누구나 임의 프로필 변조** | HIGH | 기존 결함 | ✅ `a9ab076` |
| 2 | `feedbacks` 공개 조회 → **탈퇴 사유 전체 노출** | MEDIUM | 이번 세션에서 유입 | ✅ `33a1893` |
| 3 | 클라이언트 하드코딩 관리자 비밀번호 `"admin1234"` | MEDIUM | 기존 결함 | ⏸ **미조치** |

### 2.1 profiles 임의 변조 (HIGH)

- **재현**: 사용자 A의 익명 토큰으로 B의 행을 PATCH → **HTTP 204**, `points` 999999 / `nickname` `PWNED` 변경 성공.
- `points`는 추천(+500P)·주차 보상의 재화 → **보상 체계 전체가 무의미**했다.
  anon 키는 클라이언트 번들에 공개되므로 진입 장벽도 없다.
- **수정**: `auth.uid() = id` (USING + WITH CHECK), 무제한 INSERT 정책 제거.
- **호환성**: 앱의 profiles 쓰기는 `App.jsx`/`useUserStore`/`useRideSession` 전부 본인 id 기준.
  보상 RPC `grant_referral_reward`는 `SECURITY DEFINER`라 RLS를 우회하므로 영향 없음.
  적용 후 프로필 **70 → 82행**으로 생성이 계속 동작함을 확인.

### 2.2 feedbacks 노출 (MEDIUM)

Phase 19 원본 SQL의 정책을 그대로 가져왔는데, 주석("관리자/조회용으로만 SELECT 허용")과 달리
`USING (true)` + anon GRANT였다. 앱은 쓰기만 하므로 클라이언트 조회 경로를 닫았다.
(익명 SELECT → 401, 익명 INSERT → 201, service_role SELECT → 200)

### 2.3 ⏸ 미조치 — 관리자 역할 부재 (**나중에 처리하기로 함**)

- [`UserProfileSheet.jsx`](src/components/common/UserProfileSheet.jsx)의 관리자 진입이
  **클라이언트 문자열 비교 `pwd === "admin1234"`** 하나로만 막혀 있다. 번들에서 그대로 읽힌다.
- DB에 관리자 역할이 없어 `AdminDashboard`가 동작하는 이유가 곧 **`profiles` SELECT가 열려 있기 때문**이다.
- 그래서 `profiles`의 SELECT는 이번에 조이지 않았다 — 조이면 대시보드가 멈춘다.
- **다음 작업 순서**: DB에 관리자 역할 도입 → `profiles` SELECT를 `본인 OR 관리자`로 제한 → 클라이언트 비밀번호 제거.

> 정상 확인: `hazards`·`rides`·`ride_paths`·`near_miss_events`는 RLS가 제대로 걸려 익명 조회가 필터링된다.

---

## 3. 고친 기능 결함

| # | 결함 | 커밋 |
|---|---|---|
| 1 | 운영 `feedbacks` 테이블 누락 → 탈퇴 사유 수집 실패 | `865ddf7` |
| 2 | **지역 전환 버그** — 천안↔아산 전환해도 PM 스테이션·장소 검색이 이전 지역 좌표 기준 | `1dbf88c` |
| 3 | **기상 감지가 천안 고정 좌표** + 초기 1회만 조회 | `d32f690` |
| 4 | **API 실패 시 `Math.random() < 0.3`으로 가짜 기상 경고** 생성 | `d32f690` |
| 5 | **카카오 검색 레이스** — 응답 역전 시 이전 검색어 결과가 화면에 남음 | `2880f81` |
| 6 | **번역 사전 로딩 hang 시 백지 화면** (이번에 도입했다가 발견) | `9568b3b` |
| 7 | **추천 가입 연결이 조용히 실패** | `5c4fb0e` |
| 8 | **주행거리·평균속도 1000배** | `868bc37` |
| 9 | **헬멧 스테이션 반경 필터가 항상 실패** | `868bc37` |
| 10 | **CO₂ 계수가 화면마다 제각각** | `b684685`, `6cd8a81` |

### 3.1 기상 감지 (`d32f690`)
- `useSafeData`가 **천안 단대호수 고정 좌표(36.833, 127.179)** 로만 조회 → 아산에서 주행해도 천안 날씨.
- 초기 1회만 조회 → 주행 중 비가 와도 감지 안 됨.
- 실패 시 **30% 확률로 위험 경고를 만들어내던 테스트 기믹** 제거.
  근거 없는 안전 경고는 경고 자체의 신뢰를 무너뜨린다.
- 수정: 현재 위치 기준(없으면 지역 중심 폴백), **10분 주기 갱신**,
  좌표를 **약 5km 격자로 스냅**해 초 단위 GPS마다 API를 두드리지 않게 함.

### 3.2 백지 화면 (`9568b3b`) — 이번에 만들었다가 잡은 것
- i18n 동적 로딩(`e5e27bf`) 도입 시 첫 렌더가 사전 로딩을 기다리게 했는데,
  **`import()`에는 타임아웃이 없다.** 통신이 멈추면 resolve도 reject도 되지 않아
  `catch`로도 못 잡고 → **화면이 영영 백지**.
- 수정: `i18nReady`를 2.5초 타임아웃과 race + `main.jsx`에 5초 최후 안전장치·이중 렌더 방어.

### 3.3 추천 가입 연결 (`5c4fb0e`)
- **PostgREST의 UPDATE에는 order/limit이 먹지 않는다.** `.order('landed_at').limit(1)`은 무시되고
  조건에 맞는 **모든 행**을 고치려 들어, 미청구 landing 행이 2개 이상이면
  `invitee_user_id` 유니크 제약 위반으로 **409 실패**.
- supabase-js는 예외를 던지지 않아 `try/catch`가 무의미 → 실패해도 로그조차 없었다.
- 수정: select로 1건을 고른 뒤 **id로 지정 update**, 각 단계 `error`를 실제로 확인.

### 3.4 거리 단위 (`868bc37`)
`calculateDistance`는 **미터**를 반환하는데 호출부마다 해석이 갈려 있었다.
전수 확인해 **잘못된 3곳만** 고쳤다.

| 위치 | 증상 |
|---|---|
| `App.jsx` 주행 거리 누적 | 미터로 누적한 값을 km로 소비 → **거리·평균속도 1000배** |
| `HelmetReturnSheet` ×2 | `* 1000` → 100m 지점이 **100,075m**로 표시 |
| `HelmetStationSelector` | 위와 동일. **반경 필터가 항상 실패**해 "반경 내 없음" 폴백이 늘 떴다 |

**그대로 둔 곳**(이미 미터로 올바름): 스트레스존 반경(150·200m), 합법 주차 판정(`<= 10`),
`useAutoCheckout`(`< 10`), `FavoriteStations`, `SafeCorridorSheet`, `RideStartScreen.estimateEtaMin`.

`generateMockRide`는 원래부터 km로 생성 → 실제 주행과 테스트 주행의 단위가 이제 일치한다.
운영 `rides`·`ride_paths`는 **0행**이라 마이그레이션 불필요. 단말 `localStorage`의 기존 기록은 옛 단위로 남는다.

### 3.5 CO₂ 계수 (`b684685` → `6cd8a81`)

같은 지표를 화면마다 다른 계수로 계산했다 — `useRideSession`·`App`·`ShadowImpactSheet`는 **0.2**,
`generateMockRide`는 **0.13**. 둘 다 근거가 없었다.
[`src/config/emissions.js`](src/config/emissions.js) 하나로 모으고 호출부 7곳을 교체했다.

**채택 값: 0.100 kg/km (순 절감량)**
```
125.2 g/km (승용차 실적 평균)  −  약 25 g/km (PM 생애주기)  ≈  100 g/km
```
- 승용차 125.2 g/km: 2020년 국내 판매 10인승 이하 승용·승합차 **실적 평균**
  (환경부 자동차 온실가스 관리제도). 같은 해 기준치 97 g/km를 29% 초과한 실측 평균이라
  제도 기준치보다 실제 주행에 가깝다.
  [정책브리핑](https://www.korea.kr/news/policyNewsView.do?newsId=148908381)

> ⚠️ **차감값 25 g/km는 유리한 쪽 가정이다.** PM 생애주기 배출은 편차가 크다 —
> 개인 소유·장수명 기기 20~25 g/km, **공유형 30~124 g/km**(제조·재배치가 지배적).
> 재배치가 잦거나 기기 수명이 짧은 공유 운영에서는 순 절감이 훨씬 작아지고
> 최악의 경우 0에 가까워질 수 있다.
> 대외 문서에 인용할 때는 **'승용차 대체 기준 순 절감량, PM 배출 25 g/km 가정'** 전제를
> 함께 밝혀야 정확하다. 실증 데이터가 확보되면 `emissions.js` 한 줄만 고치면 된다.

---

## 4. 성능 — 초기 임계 경로 gzip 479 KB → 272 KB

| 단계 | 초기 JS (gzip) | 커밋 |
|---|---|---|
| 시작 | 479 KB (단일 청크) | — |
| 벤더 분리 + QR 지연 | 357 KB | `7fabc5d` |
| i18n 언어별 분리 | 314 KB | `e5e27bf` |
| 시트 18종 지연 로딩 | **272 KB** (8개 청크) | `6181a32` |

### 4.1 지연 로딩에서 배운 것
- **`React.lazy`만 걸고 항상 렌더하면 첫 렌더에 청크를 받아버려 의미가 없다.**
  반드시 열릴 때만 트리에 넣어야 한다.
- 그렇다고 `isOpen && <Sheet/>`로 두면 닫는 순간 언마운트돼
  **AnimatePresence의 exit 애니메이션이 사라진다**(시트가 툭 끊기며 사라짐).
- 그래서 [`LazyMount`](src/components/common/LazyMount.jsx)를 만들었다 —
  **처음 열릴 때 받아오고 그 뒤로는 마운트 유지**. Suspense fallback도 `null` 대신 스피너를 둬,
  청크가 늦을 때 "아무 일도 일어나지 않은 것"처럼 보이지 않게 했다.
- **주행 시작 안전 경로**(`RideStartScreen`·`EarphoneConfirmGate`·`HelmetDetectionCamera`)는
  청크 지연이 곧 안전 절차 지연이므로 **정적 로딩 유지**.

### 4.2 i18n 언어별 분리
`ko.js/ja.js/en.js/zh-CN.js`로 나누고 기본어 ko만 정적, 나머지는 동적 로딩.
원본과 **4개 언어 2,785개 키 전수 대조**로 완전 일치 확인.

**i18next 설정 함정 두 가지 (실측으로 확인)**
- **`nonExplicitSupportedLngs`** — 지역 코드를 떼어내 비교하는 탓에 `zh-CN`이 영영 매칭되지 않고 ko로 폴백된다. 쓰지 않는다.
- **`supportedLngs`** — 지정하면 init 단계에서 `en-US`가 곧바로 fallback(ko)으로 치환돼 정규화할 원본을 잃는다. 지정하지 않고 `normalizeLanguage`로 직접 처리한다.
- 부수 효과: `zh-TW`가 기존엔 한국어로 떨어지던 것이 이제 중국어에 매칭된다.

### 4.3 서비스 워커 precache는 유지하기로 결정
첫 방문 시 SW가 전 청크를 배경에서 받아 **오프라인에서도 QR·다국어가 동작**한다.
주행 중 통신이 끊길 수 있는 앱 성격상 이쪽이 맞다.
즉 **임계 경로는 −43%, 총 다운로드 바이트는 유지**가 정확한 표현이다.

### 4.4 미사용 의존성 제거 (`9b20110`)
`axios`, `html-to-image`, `onnxruntime-web` — `src` 어디에서도 import되지 않았다.
번들엔 원래 없었고 설치 용량 약 8MB 감소.

---

## 5. 코드 품질 — lint 에러 58 → 0, 경고 14 → 0

### 5.1 ESLint 설정 자체가 틀려 있었다 (`2880f81`)
`eslint-plugin-react`의 **`jsx-uses-vars`가 없어** `<motion.div>`처럼 JSX에서만 쓰이는 식별자
**16개를 전부 '미사용'으로 오탐**하고 있었다. 이 오탐을 믿고 import를 지우면 앱이 깨진다.
(실제로 지웠다가 원복했다.)

### 5.2 React 19 규칙 위반
- effect 내 동기 setState → **렌더 중 조정** 패턴으로 전환.
  `prev` 상태 초기값을 마운트 시 첫 실행이 유지되도록 잡아야 한다.
- `useSpeechRecognition` 렌더 중 ref 갱신 제거, 지원 여부는 lazy 초기화.
- `HelmetDetectionCamera` cleanup이 참조하는 `stopStream`의 TDZ 위험 제거.
- 죽은 상태 제거(`SafetyQuiz.hasSpokenInit`), 파생 가능한 상태 제거(`HardwareStatusOverlay.stepText`).

### 5.3 exhaustive-deps는 건별 판단 (`1dbf88c`)
일괄 억제하지 않고 하나씩 판단했더니 **실제 버그 2건**(지역 전환)이 나왔다.
- **채운 곳**: `vibrate`(useCallback으로 안정), `t`, MapContainer useMemo(라벨이 t를 씀).
- **억제한 곳**(이유를 주석으로 명시): `mapBounds`/`regionMeta.center` 객체 동일성 오탐,
  `initAuth` 마운트 1회 부팅 시퀀스, QRScanner(넣으면 스캐너 재시작),
  MapContainer:250(넣으면 언어 변경 시 경로 재계산).

---

## 6. 검증 방법 (재현용)

| 대상 | 방법 |
|---|---|
| 다국어 | 헤드리스 Chrome + `?lng=` 로 ko/ja/en/zh-CN/en-US/ja-JP/zh-TW/ko-KR/fr **9종** 확인 |
| 기상 위치 반영 | CDP `Emulation.setGeolocationOverride`로 위치를 바꾼 뒤 요청 URL 캡처<br>(제주 → `33.5/126.55`, 아산 배방 → `36.8/127`) |
| 백지 화면 | `dist`를 서빙하되 **언어 청크 응답만 보류**하는 서버 + CDP `Runtime.evaluate`로 벽시계 10초 뒤 DOM<br>(`--dump-dom`은 load를 기다려 못 씀) → 수정 전 `rendered:false`, 수정 후 `rendered:true` |
| 추천 연결 | 로컬 Supabase REST에 미청구 행 2건 생성 후 PATCH 비교 → order+limit `409`, id 지정 `204` |
| RLS 취약점 | 익명 세션 2개를 만들어 A 토큰으로 B 프로필 PATCH → 수정 전 변조 성공, 수정 후 값 그대로 |
| 지연 로딩 | CDP로 초기 요청 청크 목록 확인(8개) + 버튼 클릭 시 해당 청크만 로드되는지 |
| 거리 계산 | 단국대→천안터미널 3,127m, 위도 0.0009° = 100m로 실좌표 대조 |

> ⚠️ 함정 둘:
> 1. 로컬에서 CDP `Fetch` 가로채기가 안 먹으면 **서비스 워커가 캐시에서 내주고 있는 것**이다.
>    새 user-data-dir로 크롬을 띄우거나 SW를 우회할 것.
> 2. `pkill -f`가 Windows에서 node 프로세스를 못 잡는다. **포트 기준으로 정리**할 것
>    (`Get-NetTCPConnection -LocalPort N | Stop-Process`). 이걸 몰라 이전 서버가 살아 있는 채로
>    테스트해 잘못된 결론을 낼 뻔했다.

---

## 7. 커밋 (17개, 머지 포함 25개)

**운영 DB·보안**
- `865ddf7` 운영 DB `feedbacks` 복구 + 마이그레이션 baseline
- `33a1893` feedbacks 공개 조회 정책 제거 — 탈퇴 사유 노출
- `a9ab076` profiles 쓰기를 본인 행으로 제한 — 임의 프로필 변조 차단

**기능 결함**
- `2880f81` lint 에러 58건 제거 + React 19 규칙 위반 정리
- `1dbf88c` exhaustive-deps 정리 — 지역 전환 버그 2건 포함
- `d32f690` 기상 위험 감지 위치 기반 + 근거 없는 경고 제거
- `9568b3b` 번역 사전 로딩 hang 시 백지 화면 방지
- `5c4fb0e` 추천 가입 연결이 조용히 실패하던 문제
- `868bc37` 거리 단위 불일치 3곳 — 주행거리 1000배, 반경 필터
- `b684685` CO₂ 계수 일원화 + 근거 명시
- `6cd8a81` CO₂ 계수를 순 절감량 0.100 kg/km로 변경

**성능**
- `7fabc5d` 초기 번들 gzip 479KB → 357KB
- `9b20110` 미사용 의존성 3개 제거
- `e5e27bf` i18n 언어별 분리 + 동적 로딩
- `6181a32` 시트 18종 지연 로딩 — 314KB → 272KB

**문서**
- `3da0767` SESSION_2026-08-29 추가 + README 갱신
- `93e59c7` 보안 점검 결과 기록

---

## 8. 주요 파일

| 파일 | 변경 |
|------|------|
| `eslint.config.js` | `eslint-plugin-react` + `jsx-uses-vars` (오탐 제거) |
| `vite.config.js` | `manualChunks` 벤더 분리 |
| `src/main.jsx` | `i18nReady` 대기 후 렌더 + 최후 안전장치·이중 렌더 방어 |
| `src/locales/i18n.js` | 언어별 동적 로딩, `ensureLanguage`/`changeLanguage`/`normalizeLanguage`, 타임아웃 race |
| `src/locales/{ko,ja,en,zh-CN}.js` | 언어별 사전 분리 (신규) |
| `src/components/common/LazyMount.jsx` | 시트 지연 로딩 헬퍼 (신규) |
| `src/config/emissions.js` | CO₂ 계수 단일 출처 + 근거 (신규) |
| `src/hooks/useSafeData.js` | 기상 조회 위치 기반 + 10분 갱신 + 가짜 경고 제거 |
| `src/App.jsx` | 시트 지연 로딩, 거리 단위, 추천 가입 연결, deps 정리 |
| `src/components/map/MapContainer.jsx` | 지역 전환 시 스테이션·검색 재조회 |
| `src/components/map/MapSearchBar.jsx` · `RideStartScreen.jsx` | 검색 결과 레이스 방지(검색조건 키) |
| `src/components/common/HelmetReturnSheet.jsx` · `HelmetStationSelector.jsx` | 거리 단위 |
| `supabase/migrations/202608291*.sql` | 스키마 캡처 · `feedbacks` 복구 · RLS 2건 |

---

## 9. 남은 것 / 알려진 사항

### 다음에 할 것
- ⏸ **관리자 역할 도입** — 2.3 참조. 하드코딩 비밀번호 제거 + `profiles` SELECT 제한을 함께 처리.
- `PROPOSAL.md`는 **커밋하지 않고 로컬 보관 중**(사용자 결정).
  헬멧 AI 서술은 실제 구현 상태에 맞게 수정해 두었다.

### 알려진 사항
- **온디바이스 AI 헬멧 판정은 미동작.** `HelmetDetectionCamera`는 주석 그대로 시뮬레이션이며
  `onnxruntime-web`은 어디서도 import되지 않는다(그래서 의존성에서 제거).
  PWA의 ONNX 캐싱 규칙과 `public/models`의 더미 모델(4KB)은 향후 연동 대비로 남겨둠.
- `index.js`가 아직 gzip 87.6KB — 추가 분할 여지는 있으나 효과 대비 복잡도가 커지는 구간.
- 기상 API는 open-meteo 무인증 엔드포인트를 그대로 호출한다.
