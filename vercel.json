{
  "buildCommand": "npm install --prefix c-pm-safety && npm run build --prefix c-pm-safety",
  "outputDirectory": "c-pm-safety/dist",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
