# C-Safe 세션 작업 정리 — 2026-09-11

> 2026-09-06 세션에서 남겨둔 **보안 백로그 전면 처리**. 최우선인 포인트 자기 위조
> 취약점 봉인부터 시작해, 추천 보상 인플레이션·제보자 익명 uid 누출·안전점수 이력
> 방치까지 순서대로 마무리했다.
>
> 커밋 7개 · 운영 DB 마이그레이션 6건 · 프로덕션 배포 다회 · 관리자 계정 최초 등록

---

## TL;DR

| 항목 | 결과 |
|---|---|
| `profiles.points`/`total_distance` 자기 위조 | **봉인** — 서버 RPC로만 변경, 직접 UPDATE 403 |
| `safety_score` | 봉인 + **이력 기능 활성화**(방치 테이블 → 정식 RPC/로그) |
| 추천 보상 무한 farming | **레이트 리밋** — 초대자별 24h 5건 / 누적 50건 |
| 제보자 익명 uid 노출 위험 | reporter_id를 **비공개 테이블 분리** + 제보 24h 10건 제한 |
| 관리자 대시보드 진입 불가(레코드 0) | `eun035@gmail.com` **최초 관리자 등록** |

**핵심 원칙**: 적립·점수 변경을 서버 SECURITY DEFINER RPC로 옮긴 **뒤** 컬럼 보호
트리거로 봉인하는 순서. 봉인 판별은 owner가 아닌 트랜잭션-로컬 GUC 플래그
(`app.points_write`)로 하여 함수 owner 불명에 의존하지 않는다.

---

## 1. 포인트 자기 위조 봉인 (최우선)

**문제**: `profiles` UPDATE RLS가 행만 제한(`auth.uid()=id`)하고 컬럼 제한이 없어,
로그인(익명 포함) 사용자가 자기 `points`/`total_distance`를 임의 PATCH할 수 있었다.
적립도 클라이언트 "SELECT 후 +N upsert" 방식이 5곳(App.jsx) + 주행완료 폴백에 흩어져 있었다.

**봉인 전략**: 정당한 적립 RPC가 `set_config('app.points_write','1',true)`를 세팅하고,
BEFORE UPDATE 트리거가 플래그 없이 `points`/`total_distance`(후에 `safety_score`)가
바뀌면 42501(→HTTP 403)로 거부.

**2단계 배포**(구 프론트 세션 드레인 고려 — 트리거를 먼저 켜면 구 세션 적립이 죽음):
- **Stage 1** (`6b7ed61`, 마이그레이션 `20260910090000`): 적립을 서버 RPC로 일원화.
  - `award_points(amount, distance, reason)` 신규 — auth.uid() 고정, 상한 1~2000.
  - `increment_user_stats(uuid,int,float)` 재정의 — 인자 무시하고 auth.uid() 강제(로컬==운영 정합).
  - `grant_referral_reward()` 재정의 — 플래그 세팅 추가.
  - App.jsx 5곳(미션/QR300/퀴즈500/헬멧100/하이브리드50) + useRideSession 폴백 삭제.
- **Stage 2** (`6d911ce`, 마이그레이션 `20260910090100`): `guard_points_write` 트리거 봉인.
- **후속 정리** (`1f1d19e`, 마이그레이션 `20260911010000`):
  - 구버전 `grant_referral_reward(uuid)` DROP(하위호환용으로 남겼던 것).
  - 트리거에 `safety_score` 봉인 추가.
  - `award_points` 실패 시 사용자 toast(`app_reward_failed`, 4개 언어).

---

## 2. 관리자 계정 등록 (운영 조치)

관리자 코드/보안은 2026-09-06에 완성돼 있었고 남은 건 운영 2단계뿐이었다.
- 대시보드에서 `eun035@gmail.com` 계정 생성(Auto Confirm) — 사용자 직접.
- `admin_users`에 uid `b075b71d-4e2a-44ac-b85b-57c62a47a7e5` INSERT — Management API로 처리.
- `is_admin()` = true 검증 완료. 이제 앱에서 관리자 로그인 시 대시보드 진입 가능.

---

## 3. 추천 보상 인플레이션 방어

**문제**: 익명 계정 무제한 생성 → 각 계정이 `referred_by_code` 설정 후 첫 주행 시
초대자에게 매번 +500. 자기 코드로 farming하면 초대자 포인트 무한 팽창.

**방어** (`2c89c3a`, 마이그레이션 `20260911020000`): `grant_referral_reward()`에
초대자(`inviter_code`) 단위 보상 상한 — **24h 롤링 5건 + 누적 50건**. 초과 시 미지급 +
`reason='rate_limited'`. 상한값은 함수 내 `v_daily_limit`/`v_total_limit` 상수.
클라 변경 없음. (익명 가입 자체의 IP/기기지문 제한은 더 무거워 후속으로 분리.)

---

## 4. 제보자 비공개 저장 + 제보 레이트 리밋

**문제**: `hazards`는 공개 SELECT + realtime이라 INSERT가 전 클라이언트에 `payload.new`
전체 행으로 브로드캐스트된다. `reporter_id` 컬럼을 두면 제보자 익명 uid가 노출된다
(컬럼 GRANT는 realtime에 안 먹히고, 구독이 base 테이블이라 뷰로도 못 막음).

**설계** (`9df6ed0`, 마이그레이션 `20260911030000`): reporter_id를 hazards가 아닌
별도 비공개 테이블에 저장.
- `hazard_reporters(hazard_id, reporter_id, created_at)` — RLS ON·정책 0개·realtime 미포함
  (service_role 전용). `supabase_realtime`은 `puballtables=false`라 자동 포함 안 됨.
- `report_hazard(...)` SECURITY DEFINER RPC — reporter_id=auth.uid() 서버 고정,
  hazards+hazard_reporters 원자적 기록, **제보자별 24h 10건** 레이트 리밋.
- hazards 직접 INSERT 정책·GRANT 제거 → 제보는 RPC로만 가능.
- `useSafeData.reportHazard`가 직접 insert → RPC 경유로 변경(현재 제보 UI 미연결).
- 관리자 제보자 조회 RPC(`is_admin` 게이트)는 후속 과제.

---

## 5. 안전점수 이력 활성화

**문제**: `safety_score_logs`가 정책 0개·쓰기 경로 0개·참조 0건으로 방치. 실제로
`profiles.safety_score`를 바꾸는 코드가 없어 기본값(80)에 고정돼 있었다.

**활성화** (`d3bc478`, 마이그레이션 `20260911040000`):
- `adjust_safety_score(delta, reason)` SECURITY DEFINER RPC — 본인 점수를 [0,100]
  클램프 증감 + 실제 반영 변화량을 `safety_score_logs`에 기록. `app.points_write`
  플래그로 트리거 봉인 통과(직접 UPDATE는 계속 차단).
- `safety_score_logs`: 본인 이력만 SELECT(RLS 정책 재추가), 쓰기는 RPC(definer)만.
- 주행완료 반영 규칙(useRideSession): 급브레이크 없음 +2 / 있음 −3, 헬멧 +1, 합법주차 +1.

---

## 6. 검증 (요약)

로컬 `db:reset`(전 마이그레이션 적용) + psql 트랜잭션 테스트, 운영은 `db query --linked`로 확인.

| 검증 | 결과 |
|---|---|
| 직접 `points`/`total_distance`/`safety_score` UPDATE | **403 (42501)** |
| 정당 적립(미션/QR/퀴즈/헬멧/하이브리드/주행완료) | 정상, 상한 초과·음수 거부 |
| `increment_user_stats` 타유저 uid 인자 | 무시, 본인만 반영 |
| 추천 보상 24h 6번째 | `rate_limited`, 미지급 |
| 제보 직접 INSERT / `hazard_reporters` 접근 | 차단 / 비공개(42501) |
| `report_hazard` 11번째 | `rate_limited` |
| hazards에 reporter_id 컬럼 | **없음**(누출 불가) |
| `adjust_safety_score` 클램프·로그·본인 SELECT | 정상 |
| `npm run build` / `npm run lint` | 클린 |

> ⚠️ 트랜잭션-로컬 GUC 주의: `set_config(...,true)`는 트랜잭션 끝까지 유지되므로
> psql에서 여러 RPC를 한 트랜잭션에 묶으면 이후 직접 UPDATE가 통과한다. 실제 앱은
> PostgREST가 요청당 1 트랜잭션·1 문이라 요청 간 누출이 없다(별도 트랜잭션 재검증 완료).

---

## 7. 남은 것

### 후속 과제
- **i18n 4개 언어 단계적 전환** — Phase 1(퀴즈/추천모달) 완료. 범위가 커 별도 세션 단위 진행 권장.
- **관리자 제보자 조회 RPC** — `get_hazard_reporter(hazard_id)` (`is_admin` 게이트). 제보자 조회 UI 필요 시.
- **익명 가입 레이트 리밋** — 추천 farming의 근본 방어(IP/기기지문). 3번 상한은 blast radius 축소일 뿐.
- **제보 UI 연결** — `report_hazard` RPC는 준비됐으나 제보 화면이 아직 미연결. 연결 시 `rate_limited` toast 안내 추가.

### 운영 조치
- **관리자 계정 정보 안전 보관**. 분실 시 복구는 `admin_users` SQL 재실행뿐. 운영자 기기에서만 로그인.

---

## 관련
- 이전 세션: `SESSION_2026-09-06.md`
- 마이그레이션 워크플로 / 관리자 셋업: `supabase/README.CLI.md` (3절 / 7절)
