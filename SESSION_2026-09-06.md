# C-Safe 세션 작업 정리 — 2026-09-06

> 2026-08-29 세션에서 **의도적으로 남겨뒀던 보안 과제**(관리자 역할 부재)의 후속 작업.
> 계획을 세우며 조사하다 **예정에 없던 기능 고장 하나를 더 찾아** 함께 고쳤다.
>
> 커밋 4개 · 운영 DB 마이그레이션 4건 · 프로덕션 배포 1회

---

## TL;DR

| 항목 | 결과 |
|---|---|
| `profiles` 전체 공개 읽기 | **차단** — 본인 행만 |
| 관리자 인증 | 클라이언트 하드코딩 → **서버 판정 + 이메일 로그인** |
| `hazards` | 정책 0개로 **죽어 있던 시민 제보·위험구역 경고 복구** |
| SECURITY DEFINER 함수 | anon 실행 전면 회수 + `search_path` 고정 |

**가장 중요한 발견**: 관리자 대시보드가 이미 대부분 목업이었다.
`hazards` 정책이 통째로 비어 있어 서버에서 아무것도 못 읽는데, 하드코딩 폴백이
화면을 그럴듯하게 채워 **고장이 드러나지 않았다.** 이런 종류가 다시 숨지 않도록
대시보드에 실측/가정값 구분을 넣었다.

---

## 1. hazards — 죽어 있던 기능 (예정에 없던 발견)

### 무슨 일이 있었나

`20260829123826_remote_schema.sql`(운영에서 pull한 스키마)이 hazards 정책 4개를
전부 DROP했는데 **대체 정책을 만들지 않았다.** RLS가 ON인 채 정책이 0개면 전면 거부다.

| 영향 | 상태 |
|---|---|
| 시민 위험 제보 저장 | INSERT 거부 → **한 건도 저장 안 됨** (운영 0행) |
| 지도 위험구역 표시 | SELECT 0행 → **마커 없음** |
| 주행 중 음성 경고 | `get_nearby_hazards`가 **SECURITY INVOKER**라 RLS를 우회 못함 → **함께 죽음** |

세 번째가 특히 중요하다. 조사 중에 `get_nearby_hazards`를 SECURITY DEFINER로 잘못
알고 마이그레이션 주석에 "음성 경고는 살아 있다"고 적었다가, `pg_proc.prosecdef`로
확인해 **`secdef=false`** 임을 알고 바로잡았다. 안전 기능의 핵심이 멈춰 있었던 것이다.

### 왜 지금까지 안 드러났나

**RLS 거부는 SELECT에서 에러가 아니라 빈 결과로 온다.**
`useSafeData.fetchHazards`는 0행을 정상으로 받아 `setLocations([])`를 했고,
폴백은 `catch` 블록에만 있어 발동하지 않았다.

### 고친 것

- SELECT 공개 / INSERT는 로그인 세션 / UPDATE·DELETE는 관리자만
- 테이블 GRANT도 함께 좁힘(anon에서 INSERT·UPDATE·DELETE 회수) — 이중 방어
- **`location` 자동 채움 트리거 추가.** 트리거가 없어 lat/lng만 저장됐는데
  `get_nearby_hazards`는 `ST_DWithin(location, ...)`으로 검색한다. 트리거가 없으면
  정책을 고쳐도 신규 제보가 음성 경고에 영영 안 잡힌다. 기존 행도 백필.
- `fetchHazards`가 0행을 이상 신호로 취급해 폴백을 태우도록 수정

> **reporter_id는 넣지 않았다.** 계획에는 있었지만, hazards는 공개 SELECT이고
> realtime 퍼블리케이션에도 포함돼 있어 컬럼을 추가하면 제보자의 익명 uid가
> 그대로 노출된다. 컬럼 단위 GRANT는 realtime 페이로드에 적용되지 않으므로
> 공개용 뷰를 따로 두는 설계가 필요하다. 후속 과제로 미뤘다.

---

## 2. 관리자 역할 도입

### admin_users + is_admin()

관리자 표시를 **`profiles.is_admin` 컬럼으로 두지 않았다.** 현재 UPDATE 정책이
본인 행의 *모든 컬럼*을 허용하므로, 컬럼 방식이면 사용자가
`PATCH /profiles?id=eq.<자기id>` 한 번으로 **스스로를 관리자로 승격**시킬 수 있다.
클라이언트 GRANT가 전혀 없는 별도 테이블로 분리했다.

```
admin_users : RLS ON + 정책 0개 + REVOKE ALL FROM anon, authenticated
              → 앱에서는 조회도 수정도 불가. service_role 전용.
is_admin()  : SECURITY DEFINER + SET search_path = public, pg_temp
```

⚠️ **RLS 재귀 주의**: 정책 안에서 대상 테이블을 다시 SELECT하면 `42P17` 무한 재귀가
난다. SECURITY DEFINER 함수가 RLS를 우회해 재귀를 끊는다. 그래서 `admin_users`
자신에는 정책을 아예 두지 않았다.

이후 **모든 정책은 `is_admin()` 하나만 참조**한다. 신원 부여 방식을 나중에 바꿔도
정책을 다시 쓸 필요가 없다.

### 이메일 로그인

익명 로그인은 JWT가 브라우저 localStorage에만 있어, 저장소를 지우면 관리자 자격이
사라지고 다른 기기에서도 못 들어간다. 그래서 이메일 로그인을 도입했다.

- `config.toml [auth.email] enable_signup = false` — 자가 가입 차단
- ⚠️ **상위 `[auth]` 의 `enable_signup`은 건드리면 안 된다.** 그걸 끄면 익명 로그인까지
  막혀 앱 전체가 죽는다. 로컬에서 익명 200 / 이메일 자가가입 400으로 확인했다.
- `AdminLoginModal` 신설. 로그인 후 `is_admin()`으로 서버가 판정하고, `admin_users`에
  없으면 즉시 로그아웃시킨다. 계정 존재 여부를 흘리지 않도록 실패 메시지는 한 가지.
- 비관리자에게는 버튼이 `B2G ADMIN CONSOLE`이 아니라 **`관리자 로그인`** 으로 보인다.
  이 분기는 UX용이고 **실제 방어는 RLS/RPC**다.

계정 설정 절차는 [`supabase/README.CLI.md` 7절](supabase/README.CLI.md)에 SQL까지 정리해 뒀다.

---

## 3. profiles 공개 읽기 차단

`Enable read access for all` `USING (true)`가 살아 있어, anon 키(번들에 공개)만으로
전 사용자의 `nickname·age·profile_image·points·safety_score·referral_code`를
페이지네이션으로 덤프할 수 있었다. profiles는 realtime 퍼블리케이션에도 포함돼 있어
**타인 프로필 변경을 실시간 스트림으로 관전**하는 것도 가능했다.

차단이 가능했던 이유:
- `profiles` 전체 읽기를 쓰는 곳은 **AdminDashboard 2줄뿐**. 나머지는 전부
  `.eq('id', user.id)`. 랭킹·리더보드가 없다 → **앱 기능 손실 0**
- profiles realtime 구독도 앱에서 쓰지 않는다(채널 3개는 각각
  `safety_grid_scores` / `near_miss_events` / `hazards` 대상)

대체 수단으로 **`get_admin_profile_stats()` 집계 RPC**를 만들었다.
개별 행을 반환하지 않는다 — **관리자에게도 원본 프로필은 주지 않는다**(최소 권한).
대시보드가 여러 형태로 조회하는 `rides`·`ride_paths`·`near_miss_events`·`zone_events`는
SELECT 정책에 `OR public.is_admin()` 예외를 얹었다.

---

## 4. SECURITY DEFINER 함수 권한 정리

대부분이 EXECUTE 제한 없이 만들어져 PUBLIC(=anon 포함)이 호출할 수 있었고
`search_path`도 고정돼 있지 않았다(하이재킹 표적).

### grant_referral_reward — 인자를 신뢰하던 문제

기존 `grant_referral_reward(p_invitee_user_id uuid)`는 인자를 `auth.uid()`와
대조하지 않았고 anon도 호출할 수 있었다. 임의 UUID로 호출하면 그 사용자의
`referrals.reward_granted`가 미리 소진되어, 정작 본인은 정당한 시점에
`already_rewarded`로 거부된다(보상 탈취는 아니지만 **서비스 방해**).

→ 인자 없는 버전으로 재작성해 호출자 본인만 처리. 프론트 호출부도 인자 제거.
구버전은 **DROP하지 않고 실행 권한만 회수**했다. 이미 배포된 앱이 인자 있는 형태로
호출 중이라 지금 지우면 추천 보상이 끊긴다. 다음 배포 주기 이후 DROP.

### 나머지

| 함수 | 조치 | 이유 |
|---|---|---|
| `refresh_rides_daily` | `is_admin()` 게이트 | 대시보드 전용 |
| `update_safety_scores` | authenticated **유지**, anon 회수 | 주행 종료 시 일반 사용자가 호출 |
| `get_near_miss_clusters` | authenticated **유지**, anon 회수 | 지도 히트맵(MapContainer) |
| `get_hazard_policy_effect`, `get_rsr_by_zone`, `get_near_miss_heatmap_geojson` | anon 회수 | 프론트 일반 경로 미사용 |

> ⚠️ **`anon`에서만 REVOKE하면 소용없다.** EXECUTE가 `PUBLIC`에 부여돼 있으면 anon이
> 거기서 상속받는다. 처음에 이걸 놓쳐 3개 함수에서 anon이 그대로 남았고,
> 권한 조회로 확인해 `PUBLIC` 회수를 추가했다.

---

## 5. 대시보드 — 실측과 목업 구분

조회가 실패해도 `avgSafetyScore=85`, `totalUsers=1`, 증감률 `12/24/-5/-2` 같은
하드코딩 값으로 화면이 채워져 **실패가 성공처럼 보였다.** hazards 고장이 오래
드러나지 않은 직접적 원인이다. 대외 시연에서 가짜 수치가 실적처럼 보이면
보안 문제 이상의 신뢰 문제다.

- `const { data }` 로 **error를 버리던 구조분해를 `{ data, error }`로 교체**
- `dataHealth` 상태로 지표별 출처 기록(`server` / `empty` / `error` / `assumed`)
- StatCard에 **`조회 실패` / `데이터 없음` / `가정값`** 배지, 헤더에 요약 배너
- 증감률은 이전 기간 스냅샷을 보관하지 않아 계산된 값이 아니므로 **`가정` 표기**

---

## 6. 커밋

- `93b3388` 관리자 역할 도입 + profiles 공개 읽기 차단 + hazards 정책 복구 (마이그레이션 3건)
- `673ad88` 하드코딩 비밀번호 제거 + 이메일 로그인 + 대시보드 실측/목업 구분
- `5586a0a` SECURITY DEFINER 함수 권한·search_path 정리
- `ace6691` 관리자 계정 설정 절차 (README.CLI.md 7절)

## 7. 주요 파일

| 파일 | 변경 |
|------|------|
| `supabase/migrations/20260906050837_admin_role.sql` | `admin_users` + `is_admin()` (신규) |
| `supabase/migrations/20260906051309_restore_hazards_policies.sql` | 정책 복구 + `location` 트리거 |
| `supabase/migrations/20260906051417_restrict_profile_reads.sql` | 공개 읽기 차단 + 집계 RPC |
| `supabase/migrations/20260906061657_tighten_function_grants.sql` | 함수 권한·search_path |
| `supabase/config.toml` | `[auth.email] enable_signup = false` |
| `src/components/common/AdminLoginModal.jsx` | 관리자 로그인 (신규) |
| `src/hooks/useUserStore.js` | `isAdmin` 상태, `refreshAdminFlag`, `signInAsAdmin` |
| `src/components/common/UserProfileSheet.jsx` | 하드코딩 비밀번호 제거 |
| `src/components/admin/AdminDashboard.jsx` | 집계 RPC + 데이터 출처 배지 |
| `src/hooks/useSafeData.js` | `fetchHazards` 0행을 이상 신호로 |
| `src/hooks/useRideSession.js` | 추천 보상 RPC 인자 제거 |

---

## 8. 검증

DB(로컬 Supabase, 운영과 동일 마이그레이션):

| 항목 | 결과 |
|---|---|
| 익명 A가 보는 profiles | **1행** (service_role은 2행) |
| 집계 RPC — 비관리자 / 관리자 | **403 → 200** (`admin_users` 등록 후) |
| `POST /admin_users` 자가 승격 | **403** |
| hazards 제보 INSERT | **201**, `location` 자동 채움 확인 |
| 비로그인 제보 / 비관리자 DELETE | **401 / 403** |
| `get_nearby_hazards` 반경 100m | **1건** (제보가 음성 경고에 잡힘) |
| RLS ON인데 정책 0개인 테이블 | hazards **제거됨** |
| 비로그인 anon RPC 호출 | `grant_referral_reward` **401**, `update_safety_scores` **401** |
| 로그인 사용자 RPC | 200 / 204 / 200 (기능 정상) |
| 비관리자 `refresh_rides_daily` | **403** |
| 구버전 `grant_referral_reward(uuid)` | **403** |

앱(헤드리스 Chrome + CDP), 로컬·운영 양쪽:
- 언어 선택 → 지도 진입 정상, ErrorBoundary 미발동, 예외 0건
- 비관리자에게 버튼 문구 **"관리자 로그인"**, 클릭 시 **로그인 모달만** 열리고 대시보드는 안 열림
- `dist` 번들에 `admin1234` 검색 결과 **0건**

운영 반영 후 `supabase db diff --linked`: **정책 차이 0건, drop 문 0건**.

---

## 9. 남은 것

### 후속 과제
- **포인트 자가 변조** — `Users can update own profile`이 컬럼 제한이 없어 사용자가
  자기 `points`를 임의 설정할 수 있다. 적립 경로 6곳(`App.jsx` 189/700/902/1560/1629,
  `useRideSession.js:430`)을 서버 RPC로 옮긴 **뒤** 컬럼 보호 트리거로 봉인하는 순서.
  트리거를 먼저 넣으면 적립이 전부 죽는다.
- **`hazards.reporter_id`** — 공개 SELECT + realtime 때문에 공개용 뷰 설계가 필요(1절 참고)
- **구버전 `grant_referral_reward(uuid)` DROP** — 다음 배포 주기 이후
- 익명 계정 무제한 생성에 따른 추천 보상 인플레이션 (레이트 리밋·기기 지문)
- `safety_score_logs` — 정책 0개(전면 차단) 상태. 앱에서 참조 0건이라 그대로 뒀다.

### 운영 조치 필요
- **관리자 계정을 아직 만들지 않았다.** [`README.CLI.md` 7절](supabase/README.CLI.md)의
  절차대로 Supabase 대시보드에서 계정 생성 → `admin_users` INSERT를 해야
  대시보드에 들어갈 수 있다. 그전까지는 누구도 못 들어간다(의도된 상태).
- 운영 `hazards`는 0행이다. 정책이 고쳐졌으므로 **지금부터의 제보는 저장된다.**
  그전까지 지도에는 폴백 위험구역 3건이 표시된다.
